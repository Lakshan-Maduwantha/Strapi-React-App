'use strict';

/**
 * contact-navigation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-navigation.contact-navigation');
