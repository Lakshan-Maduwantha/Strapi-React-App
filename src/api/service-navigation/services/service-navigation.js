'use strict';

/**
 * service-navigation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::service-navigation.service-navigation');
