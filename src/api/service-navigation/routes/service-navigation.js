'use strict';

/**
 * service-navigation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::service-navigation.service-navigation');
