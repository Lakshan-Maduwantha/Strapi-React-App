'use strict';

/**
 * about-pricing-plan service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::about-pricing-plan.about-pricing-plan');
