'use strict';

/**
 * about-pricing-plan router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::about-pricing-plan.about-pricing-plan');
