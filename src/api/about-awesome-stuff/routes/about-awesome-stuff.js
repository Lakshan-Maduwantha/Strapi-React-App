'use strict';

/**
 * about-awesome-stuff router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::about-awesome-stuff.about-awesome-stuff');
