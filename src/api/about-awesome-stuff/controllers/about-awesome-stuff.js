'use strict';

/**
 * about-awesome-stuff controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::about-awesome-stuff.about-awesome-stuff');
