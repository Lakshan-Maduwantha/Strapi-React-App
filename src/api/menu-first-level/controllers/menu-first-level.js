'use strict';

/**
 * menu-first-level controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::menu-first-level.menu-first-level');
