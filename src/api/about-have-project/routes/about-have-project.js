'use strict';

/**
 * about-have-project router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::about-have-project.about-have-project');
