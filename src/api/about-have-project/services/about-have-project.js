'use strict';

/**
 * about-have-project service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::about-have-project.about-have-project');
